# C# 集合 Part I - 字符串

## 1. 引言

在编写软件时，我们很少只处理单一的数据。大多数情况下，我们需要存储、组织和操作 **相关数据的集合**——例如用户列表、唯一 ID 集合，或者句子中的各个字符。

这就是 **集合（collections）** 的作用所在。

在 C# 中，*集合* 是指可以包含多个元素的对象，例如数组、列表或集合。集合是大多数程序的基础——它们驱动着从数据库结果、API 响应到内存缓存和文本处理的方方面面。

有趣的是，虽然我们通常认为 **字符串** “只是文本”，但从技术上讲，它是 **字符的集合**。你可以像操作其他集合类型一样对字符串进行索引、遍历和转换。这使得它成为学习 C# 集合行为的理想起点。

在这个两部分系列中，我们将探索如何高效地使用 C# 集合：

* **Part I（本篇）**：聚焦 **字符串**——它们的结构、常用操作以及性能注意事项。
* **Part II**：深入探讨 **数组、列表和哈希集合**，分析它们的差异、使用场景及底层性能表现。

通过本系列学习，你将掌握如何在内存中高效处理数据——编写更简洁、更快速、更具表达力的 C# 代码。

---

## 2. 字符串基础

C# 中的 [**字符串**](https://learn.microsoft.com/en-us/dotnet/csharp/programming-guide/strings/) 表示不可变的字符序列。声明和初始化字符串有多种方式：

```csharp
string greeting = "Hello, world!";
string empty = string.Empty;  // 等价于 ""
char firstChar = greeting[0]; // 'H'
```

C# 字符串的一些关键特性：

* **引用类型** – 虽然行为类似基本值类型，但字符串实际上是 .NET 中的对象（`System.String`）。
* **不可变** – 一旦创建，字符串无法更改。任何看似“修改”字符串的操作（如连接或替换）实际上都会返回一个 **新的字符串实例**。
* **可索引和可枚举** – 可以使用零基索引访问单个字符（`string[index]`），或使用 `foreach` 遍历。

示例：

```csharp
string word = "cat";
char c = word[1]; // 'a'

foreach (char ch in word)
{
    Console.WriteLine(ch);
}
```

---

## 3. 核心字符串操作

C# 提供了丰富的内置方法用于操作文本。下面是按类别分组的一些常用方法。

### 3.1 连接

可以使用 `+` 运算符或静态方法 `string.Concat` 来拼接字符串。

```csharp
string s1 = "Hello";
string s2 = "World";

// 使用 '+' 运算符
string result = s1 + " " + s2;          // "Hello World"

// 使用 string.Concat
string concat = string.Concat(s1, s2);  // "HelloWorld"
```

> 💡 **提示：** 对于少量字符串，使用 `+` 就够了。但如果在循环中重复构建字符串，使用 `StringBuilder` 会更高效（因为字符串不可变，每次 `+` 都会创建新实例）。

---

### 3.2 替换 / 删除 / 子字符串

这些方法可用于提取或修改字符串的部分内容。

```csharp
string text = "I like cats";

// 替换子字符串，替换所有出现的指定子字符串
string replaced = text.Replace("cats", "dogs"); // "I like dogs"

// 删除子字符串（从索引 2 开始，删除 5 个字符）
string removed = text.Remove(2, 5);             // "Ikes"

// 提取子字符串（从索引 2 开始，取 4 个字符）
string sub = text.Substring(2, 4);              // "like"
```

> 🧠 **注意：** 由于字符串不可变，这些方法都会返回 **新的字符串实例**——原始 `text` 保持不变。

---

### 3.3 拆分 / 连接

在处理结构化文本（如 CSV 或用户输入）时，拆分和连接操作很常见。

```csharp
string csv = "apple,banana,cherry";

// 按分隔符拆分 → 字符串数组
string[] parts = csv.Split(',');   // ["apple", "banana", "cherry"]

// 将数组重新连接成单个字符串
string joined = string.Join(" | ", parts);  
// "apple | banana | cherry"
```

> 🧩 **使用技巧：** `Split()` 和 `Join()` 常常一起使用，例如解析 CSV 数据或格式化显示字符串。

---

### 3.4 搜索

C# 提供多种方法在字符串中查找或检查文本。

```csharp
string sentence = "C# strings are powerful.";

// 检查子字符串是否存在
bool contains = sentence.Contains("strings");    // true

// 查找子字符串的零基索引（未找到返回 -1）
int index = sentence.IndexOf("are");             // 11

// 检查前缀或后缀
bool starts = sentence.StartsWith("C#");         // true
bool ends = sentence.EndsWith("powerful.");      // true
```

> 🔍 **提示：** 默认情况下，这些方法区分大小写。可使用重载方法（如 `Contains("STRINGS", StringComparison.OrdinalIgnoreCase)`）进行大小写不敏感的检查。

---

### 3.5 格式化与插值

C# 提供多种方式将值嵌入字符串。最常用的是 **组合格式化** 和 **字符串插值**，同时可以使用 **逐字字符串** 处理多行文本或路径。

#### 组合格式化

使用 `{0}`、`{1}` 等占位符，通过 `Console.WriteLine` 或 `string.Format` 传入参数进行替换。

```csharp
var pw = (firstName: "Phillis", lastName: "Wheatley", born: 1753);

// 使用组合格式化
Console.WriteLine("{0} {1} was born in {2}.", pw.firstName, pw.lastName, pw.born);
// 输出: "Phillis Wheatley was born in 1753."
```

> 🧠 **注意：** 在老旧代码中常用，但占位符过多时可读性下降。

#### 字符串插值 (`$`)

字符串插值是现代、可读性强的替代方案。在字符串前加 `$`，并将表达式放入 `{}`。

```csharp
var jh = (firstName: "Jupiter", lastName: "Hammon", born: 1711);

Console.WriteLine($"{jh.firstName} {jh.lastName} was born in {jh.born}.");
// 输出: "Jupiter Hammon was born in 1711."
```

> ✅ **优点：** 更清晰、类型安全、比 `{0}` 占位符更易读。

#### 逐字字符串 (`@`)

前缀为 `@` 的逐字字符串保留空格、换行和反斜杠，无需转义。

```csharp
string path = @"C:\Users\Public\Documents";
Console.WriteLine(path);
// 输出: "C:\Users\Public\Documents"

string multiLine = @"This is line 1
This is line 2
This is line 3";
Console.WriteLine(multiLine);
```

#### 结合 `$` 与 `@`

可同时使用两种前缀（`$@"..."`）实现 **插值 + 逐字**——适用于多行模板或带变量的路径。

```csharp
var user = (firstName: "Alice", lastName: "Smith");
string report = $@"User: {user.firstName} {user.lastName}
Directory: C:\Users\{user.firstName}";

Console.WriteLine(report);
/*
输出:
User: Alice Smith
Directory: C:\Users\Alice
*/
```

> 📝 **提示：**
>
> * `$` 与 `@` 的顺序无关 (`$@"..."` 或 `@$"..."` 都可)。
> * 尽量使用插值字符串 (`$"..."`) —— 更清晰、类型安全，避免索引错误。

---

### 3.6 转换与 `ToString()`

.NET 中所有类型都可以使用 `ToString()` 转换为字符串，一些特定转换（如数字 → 二进制字符串）可用 `Convert.ToString()`。

```csharp
int number = 123;
Console.WriteLine(number.ToString());     // "123"

DateTime now = DateTime.Now;
Console.WriteLine(now.ToString("yyyy-MM-dd"));  // "2025-11-10"

bool flag = true;
Console.WriteLine(flag.ToString());       // "True"
```

```csharp
int value = 13; // 二进制: 1101

// 将整数转换为二进制字符串，然后统计 '1' 的数量
int count = Convert.ToString(value, 2)
                 .Count(c => c == '1'); // 输出: 3

int num = 42;
Console.WriteLine(Convert.ToString(num, 16)); // "2a" (转换为十六进制)
```

> 🧮 **示例应用场景：** 此模式常用于编码挑战，例如统计二进制位数或格式化数值输出。

---

## 4. 性能注意事项

### StringBuilder

因为 **字符串不可变**，看似修改的操作实际上会在内存中分配 **新的字符串**。

```csharp
string s = "a";
for (int i = 0; i < 1000; i++)
{
    s += "x"; // 每次迭代都会创建新字符串
}
```

上述代码效率低下，因为每次拼接都会分配新字符串，总体表现为 **O(n²)**。

为高效处理重复修改，请使用 **`StringBuilder`**：

```csharp
var sb = new StringBuilder("a");
for (int i = 0; i < 1000; i++)
{
    sb.Append("x");
}
string result = sb.ToString();
```

👉 **核心提示：** 字符串使用简单且安全，但对于大量修改或动态内容，优先使用 **`StringBuilder`**。在内存敏感场景下，也要注意字符串驻留（interning）。

---

### 字符串驻留与内存

* **字符串驻留（interning）** 是一种机制，运行时会在全局池中存储每个 **字面量字符串** 的唯一副本。

  ```csharp
  string a = "hello";
  string b = "hello";
  Console.WriteLine(object.ReferenceEquals(a, b)); // True
  ```
* **好处：** 对重复字面量节省内存。
* **注意：** 对于运行时创建的字符串（如 `new string(...)` 或拼接）不自动驻留，除非显式调用驻留。对动态生成的字符串过度驻留反而可能 **增加内存使用**。

---

## 5. 示例：LeetCode – Replace Words

问题链接: [Replace Words](https://leetcode.com/problems/replace-words/description/)
目标: 如果字典中存在根词，用 **最短根词** 替换句子中的单词。

### **高效 C# 解法**

```csharp
public class Solution
{
    public string ReplaceWords(IList<string> dictionary, string sentence)
    {
        // 将字典转为 HashSet 以实现 O(1) 查找
        HashSet<string> dictSet = new HashSet<string>(dictionary);

        // 拆分句子为单词数组
        string[] words = sentence.Split(' ');

        // 替换每个单词为最短根词（如果存在）
        for (int i = 0; i < words.Length; i++)
        {
            words[i] = ShortestRoot(words[i], dictSet);
        }

        // 将单词重新组合成句子
        return string.Join(" ", words);
    }

    private string ShortestRoot(string word, HashSet<string> dictSet)
    {
        // 检查单词的每个前缀
        for (int i = 1; i <= word.Length; i++)
        {
            string root = word.Substring(0, i);
            if (dictSet.Contains(root))
            {
                return root; // 返回最短匹配根词
            }
        }

        // 未找到匹配根词，返回原单词
        return word;
    }
}
```

---

## 6. 总结与下一步

本文我们探讨了 C# **字符串** 的基础与实践：

* **字符串基础** – 字符串是不可变、引用类型，可像集合一样索引。
* **核心操作** – 拼接、`Replace`、`Remove`、`Substring`、`Split`/`Join`、搜索、格式化与转换。
* **性能注意事项** – 重复拼接成本高，如何高效使用 `StringBuilder`；字符串驻留对内存优化的作用。
* **实践示例** – LeetCode 风格问题（`Replace Words`）展示如何在真实场景中应用这些概念，对比直接处理与高效处理方法。

字符串在 C# 编程中无处不在，理解其行为和性能影响是编写 **干净、可读、高效代码** 的关键。

🔹 **下篇预告：** 在 **Part II** 中，我们将跳出字符串，深入 **数组、列表和哈希集合**——学习如何在 C# 中存储、组织和操作更大规模的数据集合，并了解何时选择哪种类型。

敬请期待如何将字符串中掌握的原则扩展到更广泛的集合，并编写更高效、健壮的代码！
